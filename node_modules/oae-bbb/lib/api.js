var MeetingDAO = require('./internal/dao');

var createMeeting = module.exports.createMeeting = function(displayName, description, welcomemessage){
	console.log("====================CREATING MEETING ==================");
	
	console.log(displayName,description,welcomemessage);

	MeetingDAO.createMeeting(displayName, description, welcomemessage, function(err,data){
		if (err) {
			console.log("Ocurrio un error.");
			return callback(err);
		}
		
		return callback(null, data);
	});

};

